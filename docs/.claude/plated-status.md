---
description: Report current build progress for Plated without doing any work
allowed-tools: Read
---

Read `docs/PROGRESS.md` and give me a short status report:

1. Overall completion — X of Y steps done, broken down by phase.
2. Current phase and the exact next step (ID + title).
3. Anything in "Blocked / needs your input right now" — restate exactly what I need to do for each.
4. Anything marked `done` that's worth a second look (e.g. a step that was implemented before a
   later, related decision was made — flag it, don't fix it without asking).

Keep it short. This is a status check, not a re-explanation of the whole plan.
